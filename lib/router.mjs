

export class SimpleRouter {
    method
    pattern
    handler
    constructor(method, pattern, handler) {
        this.method = method
        this.pattern = pattern
        this.handler = handler
    }
}